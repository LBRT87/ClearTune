// Placeholder — the real landing page goes here. Kept intentionally blank
// so it doesn't collide with the app shell; the working home/app entry
// point lives at /home in the meantime.
export default function RootPage() {
  return null;
}
